impl Solution {
    pub fn permute(nums: Vec<i32>) -> Vec<Vec<i32>> {
        /* could size correctly my vectors using some funny maths
        // each sub-vector is nums.len() long and then main vector is nums! / sub-vector.len() 
        */
        let size: i = std
        let res: Vec<Vec<i32>> = Vec::new();

        
    }
}
